<?xml version="1.0" encoding="UTF-8"?>
<!-- <configuration>
    <include resource="org/springframework/boot/logging/logback/base.xml" />
    <logger name="org.springframework" level="ERROR"/>
    <logger name="org.springframework" level="INFO"/>
    <logger name="com.examplespring.orderservice.api" level="TRACE"/>
</configuration> -->

<configuration>
	<include resource="org/springframework/boot/logging/logback/base.xml"/>
	<property name="LOG_LOCATION" value="c:/temp" />
	<!--<property name="LOG_LOCATION" value="." />-->
	<property name="LOG_DIR" value="logs"/>
	<property name="APP_NAME" value="order-service"/>

	<property name="CONSOLE_LOG_PATTERN" value="${CONSOLE_LOG_PATTERN:-%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>
 	<property name="CONSOLE_LOG_CHARSET" value="${CONSOLE_LOG_CHARSET:-default}"/>
	<property name="FILE_LOG_PATTERN" value="${FILE_LOG_PATTERN:-%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} ${LOG_LEVEL_PATTERN:-%5p} ${PID:- } --- [%t] %-40.40logger{39} : %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>
	<property name="FILE_LOG_CHARSET" value="${FILE_LOG_CHARSET:-default}"/>

	<appender name="STDOUT_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
		<layout class="ch.qos.logback.classic.PatternLayout">
			<!--<Pattern>%d ${APP_NAME} %-5level [%thread] %logger: %msg%n</Pattern>-->
			<pattern>${CONSOLE_LOG_PATTERN}</pattern>
		</layout>
	</appender>

	<appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOG_LOCATION}/${LOG_DIR}/${APP_NAME}.log</file>
		<!--<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>WARN</level>
		</filter>-->
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${LOG_LOCATION}/${LOG_DIR}/${APP_NAME}-%d{MM-dd-yyyy}.%i.log</fileNamePattern>
			<timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				<maxFileSize>10MB</maxFileSize>
			</timeBasedFileNamingAndTriggeringPolicy>
			<maxHistory>90</maxHistory>
		</rollingPolicy>
		<encoder>
			<charset>utf-8</charset>
			<!--<Pattern>%d ${APP_NAME} %-5level [%thread] %logger: %msg%n</Pattern>-->
			<pattern>${FILE_LOG_PATTERN}</pattern>
		</encoder>
	</appender>

	<springProfile name="local">
		<root level="debug">
			<appender-ref ref="STDOUT_CONSOLE"/>
		</root>
		<logger name="com.examplespring.orderservice.api" additivity="false" level="trace">
			<appender-ref ref="STDOUT_CONSOLE"/>
			<appender-ref ref="ROLLING_FILE"/>
		</logger>
	</springProfile>

	<!--<logger name="org.springframework.web" level="DEBUG"/>
        <logger name="com.examplespring.orderservice.api" level="trace" additivity="false">
            <appender-ref ref="ROLLING_FILE" />
        </logger>
        <root level="info">
            <appender-ref ref="ROLLING_FILE"/>
	</root>-->
 
</configuration>